---
name: applykit
description: 网申信息管家 applykit。维护可检索、可更新、可版本化的个人网申信息库并辅助填写网申，数据固定在私有目录、技能本身可被他人复用并支持 GitHub 版本更新检查。当用户说"记录一下……""更新网申信息……""查一下我之前怎么写的……""这个字段怎么填？""帮我填网申……""某公司某岗位的网申……""有冲突吗？""信息库体检/盘点""检查更新/skill 更新""保存到网申工作台……"或提到"网申""网申信息""网申工作台""简历信息库"等时使用。必须先确认固定工作目录并检索再回答，不凭记忆编造；回答必须提供可直接复制的内容；发现版本冲突必须询问用户，确认后才能更新；每次写入必须留痕。
---

# 网申信息管家

维护一个可检索、可更新、可版本化的网申信息库，并辅助用户填写网申。所有信息以工作目录中的文件为唯一事实来源，不凭记忆编造。

## 1. 核心原则（优先级从高到低）

1. 检索优先于生成：先查工作目录，查不到就明说"当前工作目录未记录"。
2. 确认优先于覆盖：新增先询问是否写入；冲突版本绝不擅自覆盖。
3. 冲突必须询问：新旧冲突时暂停，列冲突、等裁决。
4. 可复制优先于解释：答案单独成段，可直接粘贴。
5. 更新必须留痕：每次写入/更新用 `scripts\log_update.cmd` 记日志。

## 2. 信息库结构与机器约定

工作目录结构：

```
<工作目录>/
  00_总览与使用说明.md        # 总览、约定、目标画像
  01_个人基础信息.md
  02_教育经历.md
  03_实习与工作经历.md
  04_项目经历.md
  05_奖项与证书.md
  06_技能与语言.md
  07_开放问题库.md
  08_公司岗位记录/公司名_岗位名.md
  09_冲突与待确认.md
  10_更新日志.md
  11_模板/字段记录模板.md      # 字段 schema 唯一事实源
```

机器约定（写入时必须遵守，脚本依赖这些约定解析）：

- 编码：所有 .md 文件一律 UTF-8。
- 条目：以行首 `- 字段名：` 开始，条目之间用仅含 `---` 的行分隔。
- 字段前缀固定为 `- 标准答案：`、`- 短版：`、`- 长版：`、`- 关键词：`、`- 版本：`、`- 状态：`、`- 最后确认时间：` 等，完整 14 字段以 `11_模板/字段记录模板.md` 为准。
- 状态枚举仅三种：`草稿` / `已确认` / `废弃`；版本格式 `v1`、`v2`……每次内容变更 +1；日期格式 `YYYY-MM-DD`。
- 公司岗位文件命名 `公司名_岗位名.md`，去除 `\ / : * ? " < > |` 等非法字符；同公司同岗位多次投递加序号，如 `字节跳动_产品经理_2.md`。
- 数据边界：只认工作目录根下的编号文件与 `08_公司岗位记录/`、`11_模板/`；任何含 `SKILL.md` 的子目录是 Skill 本体，检索与扫描时一律排除，不得把 `assets/templates` 当用户数据。

## 3. 工作目录确认（每次调用首先执行，先于一切读写）

网申数据含个人隐私，必须固定在同一个私有文件夹内，不允许散落到临时目录、公共目录或多个项目目录。**每次触发本 Skill，在任何检索/写入之前**先运行（Windows 自带 PowerShell，无需安装 Python；统一用 .cmd 包装器调用）：

```
scripts\resolve_workspace.cmd -Cwd <当前项目目录>
```

按退出码/状态确定性分支：

| 状态 | 退出码 | 处理 |
|---|---|---|
| 已注册且有效 | 0 | 取输出中的固定目录为本次 W；在回复开头明示「本次工作目录：W」，继续后续流程；`cwd_match=false` 时说明仍以固定目录为准 |
| 尚未注册 | 3 | 请用户指定一个**固定私有目录**（推荐当前项目目录或其下 `网申工作台/`；避开公共盘、共享同步盘、临时目录和下载目录），用户明确确认后运行 `resolve_workspace.cmd -Set <路径> -Create` 注册，再继续 |
| 固定目录已丢失 | 4 | 停止一切读写，告知注册路径不存在；让用户选择「原路径重建」或「-Set 改到新路径」，不擅自换目录 |
| 配置损坏 | 5 | 展示问题，请用户重新指定，用 `-Set` 覆盖 |
| 用户要求换固定目录 | — | 必须用户明确确认后才 `-Set`；不自动迁移或删除旧目录数据，只提示用户自行迁移 |

硬性规则：

1. 固定目录之外**不读取、不写入**任何网申数据；所有脚本的 workspace 参数一律使用 resolve 确认的 W，禁止用临时目录或猜测路径。
2. 注册信息保存在本 Skill 的 `config/workspace.json`（仅本机）；W 一旦注册，后续调用默认沿用，不重复询问。
3. W 确认后检查初始化（同时存在 `01_个人基础信息.md` 与 `10_更新日志.md` 视为已初始化）；未初始化先走「流程 A 初始化」。

## 4. 意图路由

**第 0 步固定为第 3 节的工作目录确认**；随后判断用户意图走对应流程，不做多余步骤：

| 用户意图（典型说法） | 流程 | 用到的脚本/参考 |
|---|---|---|
| 首次使用 / 工作台没建 / 初始化 | A | `resolve_workspace.cmd`、`init_workspace.cmd` |
| 记录一下 / 新增 / 保存 | B | `references/category-guide.md`、`log_update.cmd` |
| 更新 / 修改 / 改一下 | C | 冲突时走 E；`log_update.cmd` |
| 怎么填 / 查之前怎么写 / 帮我填 | D | `references/output-formats.md`、`references/category-guide.md` |
| 有冲突吗 / 以哪个为准 | E | `references/output-formats.md` |
| 体检 / 盘点 / 信息库状态 | F | `scan_library.cmd` |
| 检查更新 / 升级 skill | G | `check_update.cmd`、`VERSION` |

脚本在 `scripts/` 下，每个脚本同时提供 `.ps1`（PowerShell 源码）与 `.cmd`（免安装包装器，内部以 `-ExecutionPolicy Bypass` 调用 ps1）。Agent 统一调用 `.cmd`：`resolve_workspace.cmd`（每次先确认固定目录）、`init_workspace.cmd`（幂等初始化）、`log_update.cmd`（追加日志）、`scan_library.cmd`（体检）、`check_update.cmd`（版本检查）。除 resolve/check_update 外，其余脚本第一个参数传第 3 节确认的 W，参数用单横线（如 `-File`、`-Set`、`-Json`）。运行环境为 Windows 自带 PowerShell 5.1+，无需安装 Python 或其他语言环境。

## 5. 标准流程

### A. 初始化与持续录入（幂等，可随时中断续填）

1. 先按第 3 节确认固定工作目录 W（退出码 3 时先让用户指定并 `-Set -Create` 注册）。
2. 运行 `scripts\init_workspace.cmd <W>`：自动复制全部模板，已存在文件跳过、绝不覆盖用户数据，输出 created/skipped 清单。
3. 首次初始化时一次性询问：目标行业、目标岗位、常用语言（中/英）、字数偏好、是否保存敏感信息，填入 `00_总览与使用说明.md`。
4. 空库冷启动：信息是用户逐条口述积累的，不要求一次填完。按 01→07 顺序引导，每次只收集 1-3 个字段、确认后立即落盘并记日志；用户可随时中断，下次用 `scan_library.cmd` 查看进度与"下一步建议填写"续填。
5. 用户也可随时主动说"记录一下……"，按流程 B 增量补充任意类目。

### B. 新增

1. 判定类目（01~08）；类目字段清单与写法查 `references/category-guide.md`。
2. 先检索确认无同名字段条目（避免重复新增）。
3. 按 11_模板 schema 生成条目，标准/短/长版一次写全，默认 `草稿 v1`。
4. 向用户展示拟写入内容并请求确认；确认后写入文件，运行 log_update.cmd（旧值填"（无）"）。

### C. 更新

1. 定位旧条目并读取；找不到则转 B。
2. 新旧逐字段比对：
   - 内容实质相同 → 不写入、不记日志，告知用户"无变化"（幂等）。
   - 无冲突（补充、润色、用户明确指示替换）→ 版本 +1、更新确认时间，写入并记日志。
   - 事实冲突（两个互斥值）→ 转 E，不覆盖。

### D. 检索回答

1. 解析检索维度：公司、岗位、字段、类目、字数限制、中/英文、截止时间；关键维度缺失且会影响结果时，一次问清再检索。
2. 在 W 内定位文件（排除 Skill 本体目录）；公司岗位文件支持模糊匹配，命中多个时列出候选让用户选，不猜测。
3. 选取答案：优先「已确认」且版本号最高；无已确认时取最新草稿并明确标注状态；同字段存在两个「已确认」视为数据冲突，转 E。
4. 按 `references/output-formats.md` 的检索五段式输出：【检索结果】【可直接填写】【来源文件】【版本 / 最后确认时间】【冲突或待确认】。
5. 查无记录：明说"当前工作目录未记录"，并询问是否走 B 新增。

### E. 版本冲突

1. 暂停更新，不覆盖旧内容。
2. 在 `09_冲突与待确认.md`「待确认冲突」表登记一行（字段/类目文件/旧版本/新版本/来源/发现时间/影响范围/状态=待确认）。
3. 按 output-formats.md 的冲突格式向用户列出六项并问"以哪个版本为准？"。
4. 用户裁决后：更新对应条目（版本 +1）、把 09 该行移到「已解决冲突」、运行 log_update.cmd（确认人=用户，原因写明裁决结果）。

### F. 信息库体检

运行 `scripts\scan_library.cmd <W>`（需要机器可读结果时加 `-Json`），向用户汇报：条目总数与状态分布、草稿清单、废弃清单、长期未确认清单、09 中待裁决冲突数，并按脚本输出的"下一步建议"引导续填。

### G. 版本检查与更新（见第 9 节）

运行 `scripts\check_update.cmd` 比对本地 `VERSION` 与默认 GitHub 地址的最新版本；有更新时向用户说明版本差与更新方式，经用户确认后再更新，更新不得影响 `config/` 与固定目录中的个人数据。

## 6. 异常处理矩阵

| 情况 | 处理 |
|---|---|
| 尚未注册固定目录（resolve 退出码 3） | 请用户指定固定私有目录，确认后 `-Set -Create` 注册 |
| 注册目录丢失（退出码 4）/ 配置损坏（5） | 停止读写，按第 3 节分支让用户决定，不擅自换目录 |
| 当前项目目录与固定目录不一致 | 默认仍用固定目录并告知用户；更换需明确确认 |
| 工作目录未初始化 / 缺编号文件 | 走 A；init 脚本只补缺失文件，已有数据不动 |
| 单个文件结构损坏、无法解析 | 不自动覆盖；读出可读部分给用户，确认后用模板重建该文件 |
| 模糊匹配命中多个公司岗位文件 | 列候选（文件名+岗位概况），用户选定后再继续 |
| 同字段存在两个「已确认」版本 | 按冲突走 E，不任选其一 |
| 新旧内容一致 | 幂等返回"无变化"，不写日志 |
| 写入失败或编码异常 | 统一以 UTF-8 重试一次；仍失败则报告错误与未保存内容，不谎称完成 |
| 用户问题缺关键维度 | 一次问清：公司/岗位/字段/字数/语言/截止时间 |
| 文件名含非法字符 | 按第 2 节规则清洗后命名，并告知用户 |
| 检索到 Skill 本体模板目录 | 排除，不作为用户数据 |
| 外部事实（公司信息、截止时间） | 注明来源；无法核实的标"待核实"，不编造 |

## 7. 隐私与敏感信息

- 网申信息整体属个人隐私：只在第 3 节确认的固定私有目录内落盘，不写入临时/公共/共享目录，不向固定目录之外复制。
- 身份证号、密码、薪资、推荐人/内推人联系方式等属高敏感信息：保存前先提醒并取得用户确认；条目备注标注"敏感"。
- 回答时默认脱敏（如 `138****1234`），用户明确要求才展示完整值。
- 用户在 00 目标画像中选择"不保存敏感信息"时，只在当次对话使用、不落盘。
- 更换固定目录或导出信息需用户明确指示；不主动上传、不主动分享文件内容。

## 8. 按需参考

- `references/output-formats.md`：检索五段式、新增确认、冲突报告、更新结果、体检报告的标准骨架——输出前读取对应骨架。
- `references/category-guide.md`：01~08 各类目的字段清单、写作策略与禁忌——新增/改写某类目时读取该类目小节。
- `11_模板/字段记录模板.md`（在用户工作目录内）：14 字段 schema 与版本状态规则的唯一事实源。

## 9. 版本、复用与更新

- 单人使用 / 多人复用：本 Skill 是人人可复用的"空工具"，个人数据只存在第 3 节注册的固定私有目录；Skill 文件夹内的 `config/`（本机注册信息）不随仓库分发，他人 clone 后首次使用各自注册各自目录，互不影响。
- 版本号：根目录 `VERSION` 为唯一版本源，语义化 `主.次.补丁`（如 0.1.0）；任何功能变更同步修改 VERSION。
- 检查更新：`scripts\check_update.cmd` 从默认地址 `raw.githubusercontent.com/<默认仓库>/main/VERSION` 比对（可用 `-Repo owner/repo -Branch` 覆盖）；退出码 0 最新、4 有新版本、3 网络不可达。离线时只提示无法检查，不报错中断。
- 运行时：脚本基于 Windows 自带 PowerShell 5.1+，使用者无需安装 Python；`.cmd` 包装器已内置执行策略绕过。macOS/Linux 如装了 PowerShell Core（pwsh）可直接运行 `.ps1`。
- 更新方式：优先在 Skill 目录 `git pull`；更新只覆盖技能代码/模板/脚本，`config/` 被 `.gitignore` 忽略、个人数据在固定目录中，均不受影响。更新前如本地改过技能文件，先提示用户备份。
- 不得把任何用户数据、`config/workspace.json` 提交或上传到仓库。

全程使用用户所用语言（默认简体中文），输出简洁，可复制内容单独成段。
